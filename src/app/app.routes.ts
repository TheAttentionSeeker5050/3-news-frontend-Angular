import { Routes } from '@angular/router';
import { NewsComponent } from './News/News.component';

export const routes: Routes = [
    { path: '', component: NewsComponent }  // Set NewsComponent as the default route
];
